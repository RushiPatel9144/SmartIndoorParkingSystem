<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/lightGray"
    android:orientation="vertical"
    android:padding="16dp"
    android:paddingTop="32dp"> <!-- Added paddingTop to move everything down -->

    <!-- Title for Parking Ticket -->
    <TextView
        android:id="@+id/parkingTicketTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Parking Ticket"
        android:textColor="@color/primaryText"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:layout_gravity="center"
        android:layout_marginBottom="32dp" /> <!-- Added marginBottom to separate title -->

    <!-- Success Icon -->
    <ImageView
        android:id="@+id/successIcon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center"
        android:layout_marginBottom="16dp"
        android:contentDescription="@string/success_icon"
        android:src="@drawable/smile" />

    <!-- Main Card Container -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:padding="16dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical">

            <!-- Title -->
            <TextView
                android:id="@+id/bookingConfirmationText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:text="@string/booking_confirmed"
                android:textColor="@color/primaryText"
                android:textSize="20sp" />

            <!-- Email Confirmation -->
            <TextView
                android:id="@+id/emailConfirmationText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:fontFamily="sans-serif"
                android:gravity="center"
                android:text="@string/email_confirmation_sent"
                android:textColor="@color/secondaryText"
                android:textSize="14sp" />

            <!-- QR Code -->
            <ImageView
                android:id="@+id/qrCodeImage"
                android:layout_width="160dp"
                android:layout_height="160dp"
                android:layout_marginTop="16dp"
                android:contentDescription="@string/qr_code"
                android:src="@drawable/qr" />

            <!-- Reference Number -->
            <TextView
                android:id="@+id/referenceNumberLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:text="@string/reference_number"
                android:textColor="@color/primaryText"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/referenceNumberTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="sans-serif"
                android:gravity="center"
                android:text="#4563455"
                android:textColor="@color/secondaryText"
                android:textSize="16sp" />

            <!-- Address Card -->
            <androidx.cardview.widget.CardView
                android:id="@+id/addressCard"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="4dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="12dp">

                    <TextView
                        android:id="@+id/addressTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="sans-serif-medium"
                        android:text="@string/address"
                        android:textColor="@color/primaryText"
                        android:textSize="16sp" />

                    <TextView
                        android:id="@+id/addressText"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="8dp"
                        android:fontFamily="sans-serif"
                        android:text="@string/loading_address"
                        android:textColor="@color/secondaryText"
                        android:textSize="14sp" />

                    <ProgressBar
                        android:id="@+id/progressBar"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:visibility="gone" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Footer Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center">

        <!-- Get Direction Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/getDirectionButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:text="@string/get_direction"
            app:cornerRadius="16dp"
            app:iconGravity="textStart"
            app:iconPadding="12dp"
            app:strokeColor="#369765"
            app:strokeWidth="2dp"
            app:rippleColor="#369765"
            android:textColor="#FFFFFF"
            android:backgroundTint="#369765"
            app:iconTint="#FFFFFF" />

        <!-- Cancel Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/cancelButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/close"
            app:cornerRadius="16dp"
            app:iconGravity="textStart"
            app:iconPadding="12dp"
            app:strokeWidth="2dp"
            app:strokeColor="#FF4C4C"
            app:rippleColor="#FF0000"
            android:textColor="#FF4C4C"
            android:backgroundTint="#FFFFFF"
            app:iconTint="#FF4C4C" />
    </LinearLayout>
</LinearLayout>
