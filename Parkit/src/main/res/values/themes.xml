<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartIndoorParkingSystem" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme</item>
        <item name="colorPrimaryVariant">@color/theme</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_theme</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Set translucent status bar -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="actionOverflowButtonStyle">@style/OverflowMenuStyle</item>
        <!-- Optional: Light status bar icons for a dark background -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">#000000</item>
    </style>

    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/theme</item>
        <item name="android:textColor">@color/theme</item>
    </style>
    <style name="MenuItemTextColor" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textColor">@color/theme</item>
    </style>

    <style name="OwnerTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textAppearance">@font/poppins_medium</item>
    </style>
    
    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:textColor">#FFFFFF</item> <!-- White text color for dark theme -->
        <item name="android:popupBackground">#333333</item> <!-- Dark background for dropdown -->
    </style>
    <style name="LocationFragment" parent="Base.Theme.SmartIndoorParkingSystem" >

        <item name="fontFamily">@font/poppins_medium</item>
    </style>


    <style name="Theme.SmartIndoorParkingSystem" parent="Base.Theme.SmartIndoorParkingSystem" />
  <!-- Base application theme. -->
    <style name="Base.Base.Theme.SmartIndoorParkingSystem"
        parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>